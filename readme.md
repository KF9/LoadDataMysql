# LoadDataMysql
1.Convert the ER Model  into a relational database
2. Create a mysql database schema (logical model) containing tables and attributes, enforcing primary key and foreign key constraints
3.Program in C/C++ to read data from the data files and load the data into the mysql database -  Employee, StartDate, Farm and works-on relationship. The data CANNOT be loaded directly into mysql  A PROGRAM MUST BE WRITTEN. Each record must be read individually, parsed, and the data divided appropriately to be loaded into its corresponding table.
4. Using a \T in interactive mysql print the contents of tables.
